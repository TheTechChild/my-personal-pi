node_modules/
*.log
.DS_Store
.env
.env.local
dist/
build/

# tooling caches
.cache/
*.tsbuildinfo

# Angel-internal workflow skills (infra naming, MCP tool names). Kept local so
# they are not pushed to a personal GitHub repo. Un-ignore if this repo is private.
skills/angel/
